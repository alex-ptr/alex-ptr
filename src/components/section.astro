---
const { title, id } = Astro.props
---

<section
    class="flex min-h-screen w-full flex-col items-center justify-center border-t-2"
    id={id}
>
    {title && <h2>{title}</h2>}
    <slot />
</section>
